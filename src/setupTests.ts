// import 'jest-fetch-mock';
/*
beforeEach(() => {
        fetchMock.doMock();
  /*
        fetchMock.mockResponse(req =>
            req.url === '/getDatesWithVisits' ? Promise.resolve(
                {
                    status: 200,
                    body: JSON.stringify([])
                }
            ) : Promise.resolve(
                {
                    status: 404
                }
            )
        )
        // @ts-ignore

}); */